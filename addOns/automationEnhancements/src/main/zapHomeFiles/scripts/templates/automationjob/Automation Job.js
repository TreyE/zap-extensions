/**
 * Construct a test context in which to return the script.
 * 
 * @return an {@code AutomationJob} used for testing
 */
function testContext() {
    return null;
}

/**
 * This function is called when the script is run.
 * 
 * @param job - an instance of the job under which the script is being run an {@code AutomationJob}
 * @param progress - the automation progress, an {@code AutomationProgress}
 */
function run(job, progress) {
    
}